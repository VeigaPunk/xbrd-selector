{
  "name": "@veigapunk/xbrd-selector",
  "version": "0.1.1",
  "description": "Bun packaging and test plane for the Rust xbrd-selector binary",
  "bin": {
    "xbrd-selector": "dist/xbrd-selector"
  },
  "files": [
    "dist/xbrd-selector",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "bun test",
    "package:native": "bun run scripts/package.ts",
    "package": "bun run package:native",
    "prepack": "bun run package:native"
  }
}
